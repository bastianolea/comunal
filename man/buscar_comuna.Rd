% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buscar_comuna.R
\name{buscar_comuna}
\alias{buscar_comuna}
\title{Buscar comunas por similitud}
\usage{
buscar_comuna(
  datos,
  texto,
  columna = nombre_comuna,
  similitud = 0.9,
  cantidad = 6
)
}
\arguments{
\item{datos}{Tabla de datos sobre la cual buscar, por ejemplo \link{territorios}. Debe contener una columna con nombres de comunas (especificada en el argumento \code{columna}).}

\item{texto}{Texto que quieres buscar entre las comunas}

\item{columna}{Columna de \code{datos} que contiene los nombres de comunas a comparar. Por defecto \code{nombre_comuna}. Se puede especificar sin comillas (tidy evaluation) o como texto.}

\item{similitud}{El nivel de similitud mínimo a retornar, donde 1 es total similitud y 0 es nula similitud. Por defecto es 0,9.}

\item{cantidad}{Cantidad máxima de resultados, por defecto 6. Poner \code{Inf} para mostrar todos.}
}
\value{
Mensajes respecto de la búsqueda, y el dataframe entregado en el argumento \code{datos} filtrado según comunas que cumplan con el criterio de \code{similitud} de la búsqueda.
}
\description{
Usar un término de búsqueda para obtener una tabla con comunas de nombres similares. Funciona con cualquier tabla de datos que contenga una columna con nombres de comunas, por lo que esta función puede ayudarte a encontrar comunas en fuentes externas y así confirmar o solucionar problemas. Para buscar sobre las comunas oficiales de Chile, usa \link{territorios}.
}
\examples{
territorios |>
 buscar_comuna("peña") |>
 dplyr::select(nombre_comuna, puntaje)

territorios |>
 buscar_comuna("alto") |>
 dplyr::select(nombre_comuna, puntaje)

territorios |>
 buscar_comuna("antofagasta") |>
 dplyr::select(nombre_comuna, puntaje)

territorios |>
 buscar_comuna("perro") |>
 dplyr::select(nombre_comuna, puntaje)

}
